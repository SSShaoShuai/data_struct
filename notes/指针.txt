"&" 和 "*" 运算符的说明
（1）&* pointer_1 的含义是什么
    规则：“&” 和“*” 两个运算符的优先级相同，按自又而左的方向结合
    计算：先进行* pointer_1的运算，即变量a，在执行&运算
        &* pointer_1=&a，即变量a的地址。
（1）*& a的含义是什么
    规则：先进行&a运算，得a的地址，在进行*运算。
        即&a所指向的变量，也就是变量a。
    *& a 和*pointer_1的作用是一样的，它们都等价于变量a。

